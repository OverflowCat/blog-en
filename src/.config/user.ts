import type { UserConfig } from '~/types'

export const userConfig: Partial<UserConfig> = {
  // Override the default config here
  site: { title: 'OverflowCat\'s blog' },
  seo: { twitter: '@lazy_static' },
}
